import styled from 'styled-components';

export const Field = styled.div`
  display: flex; //This is  a container for three items (Content)
  flex-direction: column;
  width: 40rem;
  height: 40rem;
  background-color: #aef3e7;
  padding: 20px;
  margin: 0 auto;
  border-radius: 0.2rem;
`;
